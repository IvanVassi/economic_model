# План: модель народного хозяйства (реконструкция сложная народнохозяйственная модель)

Дата: 2026-09-21. Область: только `SNHM.MDN` (версии 15/16/18 не рассматриваются).

> **Статус (2026-09-21):** этапы 0–2 и 4 выполнены. Парсер `mdn/` (блоки, экраны, фреймы, экспорт), эталонный
> интерпретатор `sim/` (семантика всех типов, кроме 23, подтверждена статической проверкой, §1.4), сессия «модель без
> остановки» `sim/session.py` и локальный веб-модель `ui/` (51 экран, 573 рычага, 5 переключателей режимов, графики
> с оригинальными шкалами, графики рычагов во времени, переключатель «эталон / patched»). Запуск: `python3 -m ui.server`
> → http://127.0.0.1:8765. Этап 6 начат (две правки, §5). По решению заказчика этап 3 (структурная модель) отложен.

Источники плана:
- описанига автора «Сложная народнохозяйственная модель», (51 документ) — конспекты в `docs/notes_part*.md`, сводная спецификация `docs/spec.md`;
- бинарный файл модели `SNHM.MDN` («Управление страной», 12.01.1995) и его экспорт `export/model_*.{csv,json,txt}` (`python3 -m mdn.export`);
- сопоставление описаниги и файла `docs/mapping.md` + `docs/operators_map.csv` (514 операторов), карта экранов `docs/screens_map.txt`, рычаги `docs/levers.txt`.

---

## 1. Что установлено

### 1.1 Описанига
- Модель задана **не уравнениями, а структурными схемами** схема 1–17.22 из нумерованных операторов. Явная формула одна (розничная цена продуктов питания, . Всё остальное — словесные связи «оператор → оператор».
- Архитектура: 4 блока — сфера производства (13 секторов), непроизводственная сфера (розница, наука/образование/оборона, демография), социальная сфера (20 групп), финансовый контур (бюджет, биржа + ЦБ, отраслевые банки).
- Принципы автора: **нет постоянных коэффициентов** (ставки, цены, пропорции — выходы алгоритмов); **квазистатический стартовый баланс** (модель «замирает», пока не тронут рычаг); горизонт 1–5 лет; >100 рычагов, несколько сотен наблюдаемых показателей, 50 экранов.
- Нумерация: `номер = сектор·1000 + офсет` (1032 → 13032). На схема 13–17.22 старшие разряды опущены .

### 1.2 Файл SNHM.MDN

| Показатель | Значение |
|---|---|
| Блоков | 7993 (именованных 7629), idx 1…17799 |
| Переменных состояния (типы 3, 5) | 467: секторы 306, демография 99, глобальные 35, соцгруппы 15xxx/16xxx 27 |
| Типов блоков | 17 (1, 2, 3, 4, 5, 7–17, 23) |
| Рёбер (ссылок между блоками) | 8448; висячих 0 |
| Экранов / фреймов | 51 / 248 (165 графиков, 80 панелей управления) |
| Рычагов на панелях | 555 блоков типа 2 (326 секторных, 124 глобальных, 105 демографических); всего «редактируемых» 761 |
| Секция INFOZADM | `[1][5.0][0.001][0.01][0.5]` → горизонт 5 лет, шаг интегрирования 0.001, шаг вывода 0.01 (гипотеза) |
| Алгебраических циклов без учёта уровней | 23 (см. §1.5) |

Карта диапазонов:

| Диапазон | Содержимое |
|---|---|
| 1–59 | ставки налогов/акцизов (#7–#19), собираемость (#21–#29), доходы/расходы/дефицит/профицит бюджета (#42–#59), ставка кредита #47, цены услуг/досуга (#4, #5) |
| 60–130 | натуральные заказы бюджета секторам, оплата бюджетных групп (#84–#87), режимы долга (#88, #89, #99), уставки расходных статей (#101–#117), Минфин в валюте #100, внешний долг #118–#130 |
| 131–200 | уровень жизни (#131–#148), напряжённость (#171–#188) по 18 группам, численность #153, ВВП #155/#156, эмиссия #190–#197 |
| 201–400 | спросы сферы потребления (#201–#204, #276–#299), индекс розничных цен #210, безработица (#231–#253), расходные статьи во времени (#301–#318), тарифы экспорта (#321–#337), качество жизни (#380–#398) |
| 401–500 | цены импортного сырья (#401–#413) и Фа (#421–#433), импортные тарифы (#441–#473), доли доходов бюджета (#475–#497) |
| 501–599 | валютная биржа и ЦБ (курс #501, курс ЦБ #540, резервы #523, коридор/траектория) |
| 600–780 | обмен руб↔дол у 18 соцгрупп (по 10 блоков) |
| 781–999 | импорт Фа по секторам, агрегаты спроса населения, бюджет культуры/медицины/науки/образования, потенциалы #880/#890/#891/#894, профицит #900–#946, жильё/транспорт по группам, #1000 интеллектуальный индекс |
| 1000–13999 | **13 секторов**, шаблон k000–k999 (422–461 блоков каждый); внутри — соцгруппа сектора k802–k899 |
| 15100–15599, 16100–16499 | 9 внепроизводственных соцгрупп × 92 блока (бюджетники, военные, пенсионеры, безработные, торговля; культура, медицина, наука, образование) |
| 17001–17699 | **демография**: 67 однолетних когорт по 10 блоков + агрегаты #17669–#17694 |
| 17700–17799 | родившиеся #17700; спросы/расходы 22 соцгрупп на ТНП/услуги/досуг |

Подструктура сектора (офсеты): 001–120 ядро, труд, цена, налоги, прибыль, инвестиции, фонды, депозит; 121–136 доли распределения продукции 16 потребителям; 141–156 спросы потребителей; 157–200 долги, экспорт (160–176), импорт Фа (197–200), в секторах 4/5/11 — розничный контур (159–196); 201–299 предметы труда (импорт 201–210, 8 поставщиков по 10 блоков); 361–367 депозит и переключение на погашение долгов; 400–478 «терпение» поставщиков, возврат долгов, кредит (410 сумма долгов, 442 долг банку); 801–899 соцгруппа сектора.

### 1.3 Соответствие описаниги и файла
- Совпадение по 514 операторам описаниги: **355 точно, 69 частично, 84 не совпадают, 6 отсутствуют (≈76 %)**. Правило `idx = k·1000 + off` подтверждено численно (#1025 «К сменности» с пределами 0.2…2.5 как в описаниге; #1003 = min по 8 видам сырья; #1009 = 1093 × 1025; #1029 = min(1016, 1009)).
- Где расходится: ставки налогов (описанига 11–16 → файл #14 НДС, #13 выручка, #15 прибыль, #16 фонды, #12 подоходный, #19 торговля, #18 ФОТ); биржа (книжный 500 → #501, курс ЦБ → #540); соцгруппа — **в файле другая редакция** (92 блока против 58 исходных, добавлены веса цен благ, потребности в ТНП/услугах/досуге, налог на жильё); розница — офсеты 159–196 секторов 4/5/11, а не исходные 4158–4175; добавленная стоимость #k043, а не 1042; спрос-произведение 1003×1116 — #k076; ставка депозита #230, а не 350.
- Соцгрупп в файле **22**, в описаниге 20; агрегаты по стране считаются по 18 (группы 161xx–164xx не входят в #153, #151, #152).
- **Вывод: описанига — концептуальная спецификация, файл — исполняемая истина. При конфликте следовать файлу, расхождение фиксировать.**

### 1.4 Семантика типов блоков (установлена по сырым данным и статической проверке, этап 2)

Раскладка записи блока: `S = 3 + 2a + 2aa + 2n` слотов по 4 байта: служебный int; `a` чисел + `a` ссылок группы A;
`aa` чисел + `aa` ссылок группы B; `n` коэффициентов + `n` источников Σ-входов (константа = 1); **сохранённое значение
блока** (только у типов 3, 4, 5 — это и есть состояние модели, курс = 25 руб/$, Фа = 50 и т. д.). Реализация —
`mdn/blocks.py`, `sim/model.py`.

| Тип | Блоков | Семантика | Оператор описаниги | Подтверждение |
|---|---|---|---|---|
| 1 | 972 | `Σ cᵢ·xᵢ` | сумматор | — |
| 2 | 1085 | `pa0 · Σ` — уставка-усилитель; `ra0 == idx` — признак редактируемости (761 шт.) | `K` | #12 = 12·0.01 = 12 %; #22 = 100·0.01·[#12] |
| 3 | 87 | `x' = pa0 · Σ`, состояние = сохранённое значение | `1/p` | производная ≈ 0 у 97 % в сохранённом состоянии |
| 4 | 576 | **апериодическое звено** `x' = (pa0·Σ − x)/T`, `T = pb1` (0.01…15 лет), `pb0 ≡ 1` | `1/(Tp+1)` (оператор 1002 описаниги) | сохранённое ≈ pa0·Σ у 97.5 % (медиана 1e-5); гипотеза «ограничитель» — лишь 38 % |
| 5 | 380 | `x' = pa0 · Σ`, `clip(x, pa1, pb0)`, состояние сохранено | `1/p` + ограничение | #1025: пределы 0.2…2.5; производная ≈ 0 у 99.5 % |
| 7 | 137 | `min(ra)` | min | #1029 = min(1016, 1009) |
| 8 | 13 | `max(ra)` | max | #1364 |
| 9 | 1500 | `Π(ra)` | умножитель | #1032 = 1029·1031 |
| 10 | 158 | `Σ · [ra0]` | умножитель | #1009 = 0.2·[1093]·[1025] |
| 11 | 1395 | `Π(ra) / Π(rb)` | делитель / распределитель | #89 = 42/51 |
| 12 | 572 | `Π(ra) / Σ` | делитель / свёртка | #1057 норма прибыли |
| 13 | 82 | `Σ / Π(rb)` | делитель | #61 = 6·[361]/[1039] |
| 14 | 803 | `clip(Σ, lo = pa0, hi = pb0)` | сумматор + ограничитель | #44 текущий дефицит |
| 15 | 141 | ключ: `[rb0]`, если `Σ > 0`, иначе 0 | ключ | #125 |
| 16 | 1 | **двухпозиционный ключ**: `Σ > 0 ? [rb0] : [rb1]` | ключ | #46 = режим #299 ? доходы #54 : план #90 (даёт 1770 = сохранённые расходы) |
| 17 | 69 | `x' = (Σ − x)/T`, `T = pb0`, `pa0` — начальное значение; **состояние в файле не сохраняется** → инициализируем стационаром | `1/(Tp+1)` / «годовая задержка» | #545 темп инфляции → #546 = 0 в балансе |
| 23 | 22 | **не установлено**; пока тождество (1 вход, параметр 10) | нелинейность? | только «формир доли на Жил» в 22 соцгруппах |

Порядок вычисления — по возрастанию idx, в одном проходе (блок читает уже обновлённые значения младших номеров
и прошлые — старших); интегрирование — явный Эйлер, dt = 0.001, горизонт 5 (INFOZADM).

### 1.5 Особенности, определяющие реализацию
- **23 алгебраических цикла** (после удаления уровней): контур эмиссии `#54 → #190 → #161 → #191` и шаблон `k822 → k825 → k827 → k828` («расходы на ПП / формирование доли на ПП») во всех 22 соцгруппах. Оригинал разрешал их последовательным вычислением по номеру блока, используя значения предыдущего шага — этот порядок надо воспроизвести буквально.
- Текущий экспорт `export/export17.py` пишет параметры типов 7–16 как float → у 3369 блоков не видны входы. Первая техническая задача — исправить.
- Дефекты самой модели (не описаниги): `#153` суммирует `#6811` дважды вместо `#5811`; `#961` ссылается на `#10155` вместо `#10855`; `#15168`/`#16168` ссылаются на сектор 1 вместо своей группы; `#1000` дублирует входы 880/890; у группы 161xx нет валютного блока. В эталоне сохраняются, исправляются только в отдельном варианте (§5, этап 6).
- Все числа в файле — float32. Эталон должен считаться в float32, иначе сравнение с оригиналом невозможно.

---

## 2. Цель и результат

Построить исполняемую, документированную модель народного хозяйства по образцу сложная народнохозяйственная модель в трёх слоях:

| Слой | Что это | Зачем |
|---|---|---|
| **A. Эталон** | интерпретатор СНХМ «как есть»: 17 типов блоков, порядок по idx, Эйлер, dt = 0.001, float32 | единственный источник истины о поведении оригинала; база для верификации |
| **B. Структурная модель** | та же экономика, переписанная по подсистемам с явными уравнениями и именованными параметрами; сектор — один шаблон × 13 параметризаций, соцгруппа — один шаблон × 22, когорта × 67 | читаемость, расширяемость, возможность исправлять и развивать |
| **C. Управление и наблюдение** | 51 экран как конфигурация (панели рычагов + графики), сценарии из методички описаниги | воспроизвести модель как инструмент |

Вне области: другие версии MDN; новые экономические механизмы (только как отдельный «patched»-вариант после этапа 6).

---

## 3. Состав модели по подсистемам

| № | Подсистема | Рис. описаниги | Блоки СНХМ | Уровни (состояние) | Ключевые рычаги | Главные связи |
|---|---|---|---|---|---|---|
| S1 | Технологическое ядро сектора: труд, сменность, выпуск, реализация, себестоимость | 17.1 | k001–k035, k076, k109–k119 | k025 К сменности | k020 ставка ЗП, #235 доля безработных | ← S5 (1093 рабочие места, 1080 производительность), S6 (1003 достаточность), S3 (спрос) → S2, S12 (труд), соцгруппа (ФОТ) |
| S2 | Цены, выручка, прибыль, налоги сектора | 17.1 прав., 17.4 | k036–k060, k101–k105; для 4/5/11 — k159–k196 (розница) | — | k112 рентабельность, k113 монополизм, ставки #12–#19 | → S9 бюджет (налоги), S4 инвестиции |
| S3 | Распределение продукции 16 потребителям | 17.3 | k121–k136 доли, k141–k156 спросы | — | — | ↔ все секторы, бюджет (#61–#83), экспорт, население |
| S4 | Инвестиции и переключение на погашение долгов | 17.2 | k061–k075, k078–k087, k361–k367 | k063 фонды (стоимость), k083 депозит | k064 амортизация, k066/068/070/071/075 важности, k363 порог долга | → S5 (спрос на Фа/Фп), S8 (долги), сектор 2 и 3 (заказы) |
| S5 | Производственные фонды и производительность | 17.5 | k088–k093, k100, k108 | k089 Фа, k092 Фп, k080 производительность | — | ← сектор 2 (оборудование), 3 (строительство), импорт Фа, #880 наука |
| S6 | Предметы труда: импорт + 8 поставщиков, склады, долги, «терпение» | 17.6 | k201–k299, k400–k417 | k204 импорт, k215…k275 долги (7), k219/k229/k259/k269 склады | k201/k211…k281 нормы, k299 и k400 уровни терпения, #88/#189 режим | ↔ секторы-поставщики, S8 кредит |
| S7 | ВЭД сектора: экспорт, инвалюта, импорт оборудования | 17.7, 17.13 | k157–k176, k197–k200 | k174 инвалюта сектора | k160 цена экспорта, k163 квота, k171 план, #321–#337 пошлины, #421–#433 цены Фа | ↔ S10 биржа, Минфин |
| S8 | Кредит и долги сектора | 17.8 | k401–k452 | k442 долг банку | #47 ставка, k449 ликвидность, k097/k138 интенсивности возврата, #99/#199 режимы | ↔ S4, S6 |
| S9 | Консолидированный бюджет: налоги, расходы, дефицит, секвестр, эмиссия, профицит | 17.9, 17.10 | #7–#59, #61–#117, #190–#197, #254–#257, #301–#377, #475–#497, #900–#946 | #43 дефицит накопл., #55 эмиссия, #30/#37 профицит | ставки #12–#19, собираемость #22–#28, статьи #101–#117, #191 эмиссия, важности #901–#945 | ↔ все секторы, соцгруппы бюджетников, Минфин |
| S10 | Валютная биржа и ЦБ | 17.12 | #501–#599, #420 | #501 курс, #523 резервы, #595/#596 | #509 интервенции, #529 коридор, #540 курс ЦБ, #519 резерв, #530 доля продаж, #591/#592 режим | ↔ S7, S11, S13, население |
| S11 | Минфин и внешний долг | 17.11 | #100–#130 | #100 валюта Минфина, #130 долг | #118, #122, #127, #129 | ↔ S9, S10 |
| S12 | Безработица и рынок труда | 17.21 | #231–#253, k009–k016, k029 | — | #235 | ↔ S1, соцгруппы (доход безработных), демография (#17669) |
| S13 | Социальная группа (22 экз.): доход, налоги, накопления, ПП, жильё, ТНП, услуги, досуг, уровень/качество жизни, напряжённость | 17.15–17.18 | k802–k899; 151xx–155xx; 161xx–164xx | +15 накопления, +51 жильё, +61 ТНП | нормы жилья (+93), ставка налога на жильё #7 | ← ФОТ секторов, бюджет (#84–#87, #851…#881); → спросы #201–#204, #276–#299, #131–#148, #171–#188 |
| S14 | Валюта у населения | 17.19, 17.20 | #601–#780, #1601–#1621, #807–#809 | +05 накоплено дол (18+3) | #510, #511, #561, #562, #611 | ↔ S10, S13 |
| S15 | Розничный рынок (с/х, пищепром, ТНП) | 17.14 | сектора 4, 5, 11: k159–k196; #201, #297, #4190, #4197 | — | off 171/173 (экран 6), #19 | ↔ S2 секторов 4/5/11, S13 |
| S16 | Потенциалы: наука, образование, культура, медицина; интеллектуальный индекс | 17.22 | #851–#894, #1000 | #880, #890, #891, #894 | статьи бюджета | → S5 (производительность), S13 (качество жизни), демография |
| S17 | Демография: 67 когорт, рождаемость, смертность, миграция, трудовой ресурс | модель 2 (в описаниге не приводится) | #17001–#17699 | +2 численность когорты, +7 (99 шт.) | 37 коэф. рождаемости, 66 выживаемости, #17685 миграция, #17640 условия труда | → k012 труд, соцгруппы (численность), #153 |
| S18 | Оборона (ВС) | — | сектор 6, off 180–187 | #6180 вооружения | off 181/187 | ← S3 сектор 6 |

Полный шаблон сектора — `docs/mapping.md`, приложение B; шаблон соцгруппы — приложение C; все уровни — приложение D; рычаги по экранам — приложение E.

---

## 4. Архитектура кода

```
model/
  SNHM.MDN              исходник (не трогаем)
  docs/                     описанига, спецификация, сопоставление, карта экранов
  mdn/                      слой 0: парсер формата
    sections.py             секции INFOCONT/INFOBLKS/INFOZADM/INFOFR15/INFOPICT/INFOHIST
    blocks.py               блоки: idx, type, name, coefs, sources, int-ссылки pa/pb, float-параметры
    screens.py              экраны и фреймы → панели (рычаги) и графики (наблюдаемые)
    export.py               model_blocks.json / .csv (замена export17.py)
  sim/                      слой A: эталонный интерпретатор
    ops.py                  17 типов блоков как функции (float32)
    schedule.py             порядок вычисления по idx; разбор «предыдущее/текущее значение»
    engine.py               прогон: Эйлер, dt, горизонт, запись траекторий по шагу вывода
    scenario.py             изменение рычагов во времени (ступень, график)
  econ/                     слой B: структурная модель
    sector.py               Sector(k, params): S1–S8 одним шаблоном, векторизация по 13
    social_group.py         SocialGroup(g, params): S13 (+S14), векторизация по 22
    budget.py               S9; treasury.py S11; exchange.py S10
    retail.py S15; labor.py S12; potentials.py S16; demography.py S17; defense.py S18
    params/                 YAML/JSON: все уставки и начальные значения, извлечённые из файла
    model.py                сборка, тот же интерфейс прогона, что у sim.engine
  ui/                       слой C
    screens.yaml            51 экран: панели → рычаги, графики → наблюдаемые блоки
    cli.py / notebook       прогон, сценарии, графики; позже — веб
  tests/
    test_mdn.py             парсер: 7993 блока, 8448 ссылок, все ссылки резолвятся
    test_ops.py             семантика типов на блоках-доказательствах (#1029, #1003, #1009, #1364, #61, #84…)
    test_balance.py         квазистатический баланс эталона
    test_econ_vs_sim.py     покомпонентное совпадение B с A
    test_scenarios.py       сценарии методички
  PLAN.md
```

Поток данных: `MDN → mdn.blocks → sim.engine (эталон) → траектории A`; `mdn.blocks → econ.params (извлечение уставок) → econ.model → траектории B`; `A vs B` в тестах. UI работает с обоими.

Стек: Python 3.12, NumPy (float32 для A, float64 допустимо для B с отдельным режимом float32), pytest; PyYAML/JSON для параметров; matplotlib/plotly для графиков. Веб-интерфейс — отдельным решением после этапа 4.

---

## 5. Этапы

### Этап 0. Фиксация базы
- `git init`, коммит исходников и `docs/`.
- Перенести `export/mdn.py`, `export/export17.py` в `mdn/` как отправную точку.
- Результат: репозиторий с воспроизводимой историей.

### Этап 1. Правильный экспорт (слой 0)
- Для типов 7, 8, 9, 10, 11, 12, 13, 15, 16 читать `pa`/`pb` как int-ссылки (раскладка: `a` ссылок в `iv[a+1…2a]`, `aa` ссылок в `iv[1+2a+aa…2a+2aa]`); для 2, 3, 4, 5, 14, 17 — как float.
- Разобрать INFOPICT/INFOFR15 (уже сделано в `docs/frames4.py`) и включить в парсер; разобрать INFOZADM (параметры прогона) и INFOHIST.
- Приёмка: все ссылки из `pa`/`pb` указывают на существующие блоки; у всех 3369 блоков типов 7/8/9/11 появились входы; повторный экспорт совпадает с `docs/mapping.md` на блоках-доказательствах.

### Этап 2. Эталонный интерпретатор (слой A)
- Реализовать 17 типов по §1.4; тип 23 — временно как тождество с флагом «неизвестно».
- Порядок вычисления: строго по возрастанию idx; интеграторы (3, 5, 17) — явный Эйлер с dt = 0.001, ограничения применяются после шага.
- Экспериментально определить, читает ли блок значение источника **текущего** шага (если источник уже вычислен) или **предыдущего**. Индикатор — циклы `k822…k828` и `#54…#191`: правильный вариант даёт устойчивый баланс (см. приёмку).
- Приёмка (главный тест семантики): **квазистатический баланс** — при штатных уставках и начальных значениях из файла все 467 уровней за 5 модельных лет остаются постоянными (относительный дрейф < 1e-4 в float32). Каждый дрейфующий блок — либо ошибка семантики типа, либо реальная особенность оригинала; список разобрать поимённо.
- Вторая приёмка: инварианты — `#k025 ∈ [0.2, 2.5]`, индексы достаточности `∈ [0, 1]`, `#k060 ∈ [0, 1]`, `#527/#528 ≤ 1`.
- Третья: сценарий методички (экран 9) — ступень одной налоговой ставки на ±25–50 % даёт качественно ожидаемую реакцию бюджета, цен, уровня жизни.

### Этап 3. Структурная модель (слой B)
Порядок — как в методичке описаниги , от сферы производства к социальной сфере. Каждая подсистема: явные уравнения в коде + параметры в YAML, извлечённые из файла скриптом (не вручную) + тест совпадения с A на общей траектории (сравнение всех блоков подсистемы, допуск 1e-5 относительно в float32).

1. Сектор (S1 → S2 → S3 → S5 → S6 → S7 → S8 → S4), сначала сектор 1 «сырьё», потом обобщение на 13 с различиями (4/5/11 розница, 2/3 поставки фондов k341–k357, 6 оборона).
2. Бюджет S9 + Минфин/внешний долг S11.
3. Биржа/ЦБ S10 + валюта у населения S14.
4. Соцгруппа S13 (шаблон 92 блока) + агрегаты по стране + безработица S12.
5. Розница S15.
6. Потенциалы S16, демография S17, оборона S18.

Приёмка этапа: полная модель B воспроизводит A на баланс-прогоне и на 3–5 сценариях с допуском 1e-4; список всех «магических» коэффициентов (вида 81.97, 0.007479, 0.2) собран в `econ/params/` с указанием блока-источника.

### Этап 4. Управление и наблюдение (слой C) — выполнено
- Формат INFOFR15/INFOPICT разобран строго (`mdn/screens.py`): 250 фреймов — 167 графиков (блок, шкала min/max, подпись),
  44 + 31 панели рычагов (блок, значение, диапазон), 5 переключателей режимов (пары блоков: #191/#192 эмиссия, #299/#298
  расходы по доходам/плану, #99/#98 и #199/#198 возврат долгов, #189/#88 поставки должнику, #592/#591 коридор/траектория),
  3 гистограммы демографии; 1746 элементов, все ссылаются на существующие блоки; 573 рычага, значения совпадают с уставками.
- Уставки типа 2 вынесены в массив параметров интерпретатора и меняются без перекомпиляции (`Model.set_param`).
- `sim/session.py`: прогон отрезками, история всех блоков с шагом 0.01, журнал изменений рычагов, сброс.
- `ui/server.py` + `ui/static/index.html`: стандартная библиотека и ванильный JS, без зависимостей; панели располагаются по
  координатам оригинала (сетка 640×480); у каждой серии графика своя шкала, как в оригинале, плюс авто-шкала.
- Проверено в браузере: НДС 20 → 30 % → за год дефицит 0.9 → 112, ВВП −12 %, уровень жизни 0.620 → 0.595.
- Графики изменения рычагов во времени (`Schedule` в `sim/session.py`): точки (t, значение), ступенчато или плавно,
  применяются на каждом шаге счёта; редактор с предпросмотром в панели рычага, список активных графиков, графики
  переживают сброс (эксперимент повторяется с t = 0). Тест `test_schedule_step_and_linear`.
- Графики наблюдаемых: общая ось с круглыми делениями по данным; при разрывах в порядке величины (> 8×) — до трёх сложенных
  частей со своими осями; подписи серий с текущими значениями на графике; безымянные панели-продолжения получают
  заголовок соседа с диапазоном подписей; опция «шкалы оригинала».
- Не сделано: сценарии методички как автотесты (есть только тест на НДС), сохранение/загрузка сценариев в файл.

### Этап 5. Документация модели
- Единый документ «Модель сложная народнохозяйственная модель» с фактическими номерами файла (не исходными): подсистемы, уравнения, параметры, рычаги, наблюдаемые; таблица расхождений с описанигой (из `docs/mapping.md`).
- Разобрать тип 23 (22 блока) экспериментально: подобрать функцию, при которой баланс сохраняется и «доля на жильё» ведёт себя монотонно.

### Этап 6. «Patched»-вариант — начат (`sim/patches.py`, переключатель в шапке интерфейса)
Правки применяются к копии блоков перед компиляцией и нормируются к исходному состоянию эталона, поэтому при t = 0
patched-вариант совпадает с файлом точно (тест `test_patched_equals_reference_at_start`), а меняется только динамика.
1. **Численность групп по занятости.** В файле численность группы сектора #k811 = «подано труда» #k012 (константа),
   а доход на одного #k026 делится на занятых #k029: уволенные оставались в группе с доходом занятых, группа безработных
   имела постоянную численность 1. Теперь #k811 = (#k012₀/#k029₀)·#k029, #15411 = #231/#231₀. Статья пособий #117
   остаётся планом игрока: при росте безработицы пособие на одного падает (вариант «статья растёт с числом безработных»
   рассматривался и отклонён — это решение игрока, а не модели).
2. **Предел индекса качества жизни** #k858 ≤ 5 (в исходном состоянии максимум 3.13): индекс не ограничен сверху,
   а его знаменатели ограничены снизу, одна группа уводила общий индекс страны с 0.6 до 1.9.

Проверка на сценарии «НДС 20 → 30 %, 20 лет» (`scripts/` нет, см. тесты и журнал сессии):

| Показатель, t = 15 | эталон | patched |
|---|---|---|
| качество жизни страны #380 | 1.92 (всплеск от группы пищепрома, вклад 71) | 0.53 (вклад 2.0) |
| уровень жизни #151 | 0.571 | 0.500 |
| социальная напряжённость #152 | 0.63 | 0.71 |
| безработных #231 | 22 | 37 |
| пособие на одного #87 | 8 (константа) | 1.39 |
| дефицит бюджета #44 | 0 | 308 |
| ВВП #155 | 11 510 | 8 650 |

Вывод: устойчивость эталона к шокам частично держится на артефакте — уволенные продолжают потреблять как занятые.
В patched-варианте налоговый шок даёт спираль безработицы и падение спроса, которые игроку надо гасить статьёй пособий.
Не сделано из этапа 6: агрегаты по 22 группам вместо 18 (#151, #152, #153), опечатки ссылок (#153 → #6811 дважды,
#961 → #10155, #15168/#16168), дубли входов #1000.

Зависимости: 0 → 1 → 2 → 3 → 4 → 5; 6 после 3. Этап 4 можно начинать параллельно с 3 на слое A.

---

## 6. Верификация (сводно)

| Проверка | Что сравниваем | Критерий |
|---|---|---|
| Парсер | число блоков, ссылок, типов | 7993 / 8448 / 17, 0 висячих; параметры-ссылки резолвятся на 100 % |
| Семантика типов | блоки-доказательства из `docs/mapping.md` §7.1 | вычисленное значение совпадает с ожидаемым по формуле |
| Баланс эталона | 467 уровней, 5 лет | дрейф < 1e-4; отклонения объяснены поимённо |
| Инварианты | ограниченные блоки | не выходят за пределы `pa/pb` |
| B против A | все блоки подсистемы на баланс- и сценарных прогонах | относительная ошибка < 1e-4 |
| Сценарии описаниги | реакция на ступень налога, расходов, ЗП, курса | направление и порядок величины эффекта как в описании описаниги |
| Производительность | прогон 5 лет, dt 0.001 = 5000 шагов × 7993 блока | эталон ≤ 1 мин на ноутбуке (векторизация по типам), B быстрее |

### Результаты этапа 2 (эталон, `scripts/run_indicators.py`)

| Проверка | Результат |
|---|---|
| Парсер | 7993 блока, 18 305 ссылок, 0 висячих; параметры-ссылки типов 7–16 резолвятся на 100 % |
| Согласование алгебры при сохранённом состоянии | сходится за 73 итерации до 0; делений на ноль в согласованном состоянии — 0 |
| Тип 4 (звено) | сохранённое ≈ g·Σ: <1 % у 97.5 %, медиана 1e-5; 7 отклонений — реальная динамика (жильё, демография, #161) |
| Типы 3/5 | производная <1 % от значения у 96.6 % / 99.5 %; отклонения — гашение внешнего долга (−18 $/год), накопление жилья, валюта секторов |
| Прогон 5 лет, dt 0.001 | 2.4 с (Python 3.13, без numba); nan/inf нет; дрейф состояния: <1 % у 86.6 %, <10 % у 96.7 % |
| Экономика | ВВП 11 370 → 11 330; доходы бюджета 1770 → 1761, расходы 1771, дефицит 0.9 → 10–15 без эмиссии (рычаг #191 = 0); курс 25.0 → 24.95; индекс цен 1.045 → 1.043; внешний долг 92.6 → 0 за 4 года, затем валюта Минфина 10 → 42; безработных 7.8 → 8.0; население +0.4 % |

Вывод: сохранённое состояние — квазистатическое с медленными процессами (долг, жильё, интервенции ЦБ), а не
идеально неподвижное; переходный процесс первого года (доходы −0.8 %) требует разбора (инициализация типа 17,
порядок «текущее/предыдущее», float32).

---

## 7. Риски и открытые вопросы

| Риск / вопрос | Влияние | Как снимаем |
|---|---|---|
| ~~Порядок вычисления в 23 циклах~~ | снято | проверено: последовательный и Якоби дают одинаковые траектории при dt = 0.001 (`scripts/transient.py`) |
| Тип 23 неизвестен | 22 блока «формир доли на Жил»: вход `1 + clip(доля ПП − 0.5)` ∈ [1, 1.5], выход делит долю на жильё | в сохранённом состоянии у 16 из 22 групп вход = 1, остатки у прочих не различают гипотез (x¹⁰, exp, линейная); принято тождество; вероятно, динамический блок (стационар = вход) |
| ~~Начальные значения~~ | снято | состояние типов 3/4/5 хранится в самой записи блока (хвостовой слот); тип 17 инициализируется стационаром — вариант `pa0` даёт шок ВВП +2 % |
| Остаточный дрейф первого года (доходы бюджета −0.8 %, дефицит 1 → 15) | эталон «почти», но не идеально неподвижен | сохранённое состояние содержит медленные процессы (К сменности 0.9983 → 0.9978, гашение долга); проверить на float32; сверить с описанием исходного состояния в описаниге (экран 9) |
| Метод интегрирования оригинала (Эйлер — гипотеза по dt = 0.001) | численные расхождения | сравнить дрейф баланса при Эйлере vs RK2; описанига явно про «1/p» и малый шаг — Эйлер вероятен |
| float32 vs float64 | несовпадение с оригиналом на длинных прогонах | эталон в float32; B — в обоих режимах |
| Описанига описывает другую редакцию соцгруппы и розницы | соблазн «исправить файл под описанигу» | правило §1.3: следуем файлу, расхождения — в таблицу |
| 761 «редактируемых» уставок против 555 на экранах | 206 скрытых рычагов | вынести в отдельный список; в UI — как «расширенные» |
| Дефекты оригинала (#153, #961, #15168, #1000) | эталон воспроизводит ошибки | сохраняем в A, исправляем только в этапе 6 |
| Смысл масштабных коэффициентов (81.97, 0.007479 …) | непонятные единицы измерения | в B хранить как параметры со ссылкой на блок, не «объяснять» без основания |

---

## 8. Первые шаги (ближайшая итерация)

1. ~~`git init`, коммит исходников~~ — сделано.
2. ~~Правильный парсер и экспорт~~ — `mdn/`, `python3 -m mdn.export`.
3. ~~Интерпретатор, статическая проверка, прогон 5 лет~~ — `sim/`, `scripts/static_check.py`, `scripts/run_indicators.py`, `tests/`.
4. ~~Порядок вычисления, инициализация типа 17~~ — решено (см. §7); тип 23 — тождество; float32 — не проверялось.
5. Этап 3 с сектора 1 (`econ/sector.py`), верификация против `sim`.
6. ~~Прикладная калибровка~~ — сделано (сентябрь 2026): `scripts/kz2025.py` (Казахстан-2025: ставки, коэффициенты баз #21–#29, расходы, экспорт, Нацфонд),
   `scripts/kz_vat_report.py` (сценарии НДС 12 → 16 % на 2026–2030 гг.), отчёт `docs/report_kz_vat.md`, тест `tests/test_kz2025.py`.
   Установлено: перестройка отраслевой структуры через занятость/фонды дестабилизирует модель; налоги нейтрально «переобозначаются» парой ставка × коэффициент базы;
   экспорт сектора 6 не регулируется планом (весь выпуск на экспорт); экспортные пошлины лучше не трогать.

---

## Приложение: документы в `docs/`

| Файл | Содержимое |
|---|---|
| `notes_part1_p1-17.md`, `notes_part2_p18-34.md`, `notes_part3_p35-51.md` | подробные конспекты описаниги по страницам, с расшифровкой схем |
| `spec.md` | сводная спецификация описаниги: архитектура, словарь операторов, 17 подсистем, формулы, рычаги, допущения, расхождения оригинала |
| `mapping.md` | сопоставление описаниги с файлом: правило нумерации, соцгруппа, биржа, карта диапазонов, уровни, ревизия типов с доказательствами, экраны, рычаги; приложения A–E (полные таблицы, шаблоны сектора и соцгруппы, все уровни, все рычаги по экранам) |
| `operators_map.csv` | 514 операторов описаниги → блок файла, вердикт, фактическое соответствие |
| `screens_map.txt`, `screens.json`, `levers.txt`, `panels.txt` | разбор INFOPICT/INFOFR15: 51 экран, 248 фреймов, 1545 элементов, 555 рычагов |
| `frames4.py`, `final_map.py` | скрипты разбора экранов и сопоставления |
